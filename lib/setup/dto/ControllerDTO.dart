import '../SetupController.dart';

// Object used for passing the setup controller as an object in the navigator
class ControllerDTO {
  final SetupController controller; 

  ControllerDTO(this.controller);
}